'use strict';

// SCOPING IN PRATICE
/* function calcAge(birthYear) {
    const age = 2037 - birthYear;

    function printAge() {
        const output= `${firstName}, You are ${age}, born in ${birthYear}`;
        console.log(output);

        if(birthYear >= 1981 && birthYear <= 1996) {
            var himanshu = true;
            const firstName = 'ladva';

            output = 'NEW OUTPUT!';
            const str = `Oh, and you are a himanshu , ${firstName}`;
            console.log(str);

            function add(a,b) {
                return a+b;
            }
        }
        // console.log(str);
        console.log(himanshu);
        // console.log(add(2,3)); 
    }
    printAge();

    return age;
}

const firstName = 'himanshu';
calcAge(1991); 
// console.log(age);
// printAge() */